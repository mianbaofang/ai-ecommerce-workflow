---
name: ai-ecommerce-workflow-legacy
description: Compatibility alias for the former nested package path. The canonical Skill is the repository-root SKILL.md; use this path only to discover that migration.
metadata:
  author: mianbaofang
---

# Legacy Compatibility Entry

The canonical Skill now lives at the repository root:

```text
../SKILL.md
```

Use the root entry for public market research, observed price references, comparable product links, platform copy, and optional image-generation briefs.

This nested path is retained only so older links do not silently run the former 15-item workflow. It does not define crawler, scrape, auto-install, login-state, or publishing behavior.
